# Anti-patterns and Joins

## Setup
```bash
docker-compose up -d
```

### Quiz
You can access the frontend on `http://localhost:3000`.

### Manual
You can also use the `task.http` file located in `backend/http-requests` to run the queries to the backend directly.